on:
  push:
    branches:
    - main
jobs:
  print_message:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    - name: Print a message
      run: echo "A push was made to ${{ github.ref }}!"
